# راهنمای نصب و تنظیم دفترچه تلفن Flask

## 🚀 نصب و راه‌اندازی در سرور

### 1. کپی کردن فایل‌ها
```bash
# کپی کردن تمام فایل‌های پروژه به سرور
scp -r * user@server:/path/to/project/
```

### 2. تنظیم فایل .env
در سرور، فایل `.env` را ویرایش کنید:

```bash
DB_HOST=your_mysql_host
DB_USER=your_mysql_user  
DB_PASSWORD=your_mysql_password
DB_NAME=phone_book

# تنظیمات دسترسی
# برای اجازه دادن به همه کاربران برای اضافه/ویرایش مخاطبین
DISABLE_IP_RESTRICTION=true

# برای محدود کردن دسترسی به آی‌پی‌های خاص
# DISABLE_IP_RESTRICTION=false
```

### 3. نصب وابستگی‌ها
```bash
pip install -r requirements.txt
```

### 4. اجرای برنامه
```bash
# برای تست
python app.py

# برای production با gunicorn
pip install gunicorn
gunicorn -w 4 -b 0.0.0.0:5000 app:app
```

## 🔧 تنظیمات مهم

### دسترسی کاربران
- **همه کاربران** می‌توانند مخاطبین را اضافه و ویرایش کنند
- هیچ محدودیت آی‌پی وجود ندارد
- دکمه "اضافه کردن مخاطب" برای همه نمایش داده می‌شود

## 📱 قابلیت‌ها

### صفحه اصلی
- نمایش لیست مخاطبین
- جستجوی بلادرنگ
- مرتب‌سازی ستون‌ها
- دکمه "اضافه کردن مخاطب" (در صورت داشتن مجوز)

### اضافه کردن مخاطب
- نام کامل (الزامی)
- شماره تلفن (الزامی و یکتا)
- سمت
- دپارتمان  
- مکان

### ویرایش مخاطب
- امکان ویرایش تمام فیلدها بجز شماره تلفن
- ثبت لاگ تغییرات

### پنل مدیریت
- آدرس: `/admin/login`
- پسورد پیش‌فرض: `admin123`
- تغییر پسورد
- مشاهده لاگ‌ها
- مدیریت منو
- تنظیمات دسترسی

## 🔒 امنیت

### تغییر پسورد ادمین
1. وارد پنل ادمین شوید
2. در بخش "تغییر پسورد" پسورد جدید وارد کنید
3. روی "تغییر پسورد" کلیک کنید

### کلید مخفی Flask
```python
# در app.py خط 16
app.secret_key = 'your_very_secure_secret_key_here'
```

## 🌐 API

### دریافت لیست مخاطبین (JSON)
```
GET /api/contacts
```

### پاسخ نمونه:
```json
{
  "contacts": [
    {
      "id": 1,
      "full_name": "علی احمدی", 
      "phone": "1001",
      "position": "مدیر",
      "department": "IT",
      "location": "طبقه دوم"
    }
  ],
  "count": 1
}
```

## 🐛 عیب‌یابی

### مشکل: دکمه "اضافه کردن مخاطب" نمایش داده نمی‌شود
**حل**: دکمه باید همیشه نمایش داده شود. اگر نمایش داده نمی‌شود، مرورگر را refresh کنید یا cache را پاک کنید.

### مشکل: خطای اتصال به دیتابیس
**حل**: بررسی کنید تنظیمات دیتابیس در `.env` صحیح باشد

### مشکل: صفحه کند لود می‌شود
**حل**: بهینه‌سازی‌های انجام شده شامل:
- کش کردن منو
- HTTP cache headers
- بهینه‌سازی کوئری‌ها
- حذف کتابخانه‌های غیرضروری

## 📊 آمار و گزارش

### لاگ‌ها
- تمام تغییرات ثبت می‌شود
- شامل آی‌پی، زمان، نوع عملیات
- قابل مشاهده در پنل ادمین

### بکاپ
```bash
# بکاپ دیتابیس MySQL
mysqldump -u root -p phone_book > backup.sql

# بازگردانی
mysql -u root -p phone_book < backup.sql
```

## 📞 پشتیبانی

در صورت بروز مشکل، لطفاً فایل‌های لاگ را بررسی کنید:
- لاگ Flask در terminal
- لاگ‌های سیستم در پنل ادمین